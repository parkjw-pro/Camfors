docker build -t backend-django .

sudo docker run -p 8000:8000 --name backend-django --network A304 backend-django

docker run -p 8000:8000 backend-django

sudo docker build -t frontend-vuejs .

sudo docker run -p 3000:3000 --name frontend-vuejs --network A304 frontend-vuejs


"gg=G" vi정렬


sudo docker images 현재 떠있는 이미지 확인

sudo docker ps 현재 실행중인 컨테이너 확인

sudo docker ps -a 현재 중지 중인 컨테이너 확인

sudo docker rmi <id> 이미지 삭제

sudo docker rm <id> 컨테이너 삭제



@click="goDetailList(item.campsite_id)"


@click.native="slideClicked(swiper.activeIndex)"


$ sudo docker run --name DB이름 -p 3306:3306 -e MYSQL_ROOT_PASSWORD=비밀번호 -
d mariadb

sudo docker run --name 304-DB -p 3306:3306 -e MYSQL_ROOT_PASSWORD=password -d mysql



docker inspect --format = '{{. Id}} {{.Parent}}'$ (docker images --filter since = 2aa94f133382 -q)

docker inspect --format='{{.Id}} {{.Parent}}' $(docker images --filter since=2aa94f133382 -q)

sud
 sudo docker run -p 3000:3000 --name frontend-vuejs --network A304 frontend-vuejs

$ sudo docker network ls
